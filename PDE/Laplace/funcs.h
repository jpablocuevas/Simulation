// Memory allocation 

double **alloc_2d (int, int);

void dealloc_2d (double **, int);

// Relative error

double abs_val (double);

double rel_err (double, double);

// File naming 

int count_digits (int);

char str (int);

char *int_to_char (int);

char *file_name (int);


