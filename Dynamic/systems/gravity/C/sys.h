# include "mem.h"


void sys (size_t N, ld, ld, unsigned short int);

void update (ld **, ld **, size_t);

void acc (ld *, ld **, size_t N, unsigned int i);

void pos_verlet (ld **, ld **, ld **, ld **, ld *, size_t, ld, ld);
			
/*void vel_verlet (size_t, ld, ld);

void stoermer_verlet (size_t, ld, ld);

void beeman (size_t, ld, ld);*/

void test (size_t);
